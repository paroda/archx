-- Conky, a system monitor https://github.com/brndnmtthws/conky
--
-- This configuration file is Lua code. You can write code in here, and it will
-- execute when Conky loads. You can use it to generate your own advanced
-- configurations.
--
-- Try this (remove the `--`):
--
--   print("Loading Conky config")
--
-- For more on Lua, see:
-- https://www.lua.org/pil/contents.html

conky.config = {
    alignment = 'top_left',
    background = false,
    border_width = 1,
    cpu_avg_samples = 2,
    default_color = 'black',
    default_outline_color = 'black',
    default_shade_color = 'black',
    double_buffer = true,
    draw_borders = false,
    draw_graph_borders = true,
    draw_outline = false,
    draw_shades = false,
    extra_newline = false,
    font = 'FiraCode Nerd Font Mono:size=72',
    gap_x = 3200,
    gap_y = 1600,
    minimum_height = 5,
    minimum_width = 5,
    net_avg_samples = 2,
    no_buffers = true,
    out_to_console = false,
    out_to_ncurses = false,
    out_to_stderr = false,
    out_to_x = true,
    own_window = true,
    own_window_class = 'Conky',
    own_window_type = 'override',
    own_window_transparent = true,
    own_window_argb_visual = true,
    own_window_hints = 'undecorated,sticky,skip_taskbar,skip_pager',
    show_graph_range = false,
    show_graph_scale = false,
    stippled_borders = 0,
    update_interval = 1.0,
    uppercase = false,
    use_spacer = 'none',
    use_xft = true,
}

conky.text = [[
${color 0033ff}${font FiraCode Nerd Font Mono:size=256}${alignr 1}
${color black}${font FiraCode Nerd Font Mono:bold:size=96}${voffset -280}${time %H:%M}  
${color black}${font FiraCode Nerd Font Mono:size=128}${voffset -200}${alignr}
]]


-- boxed
--${font FiraCode Nerd Font Mono:size=72}${color black}┏━━━━━━━━━┓
--┃${font FiraCode Nerd Font Mono:bold:size=72}   ${time %H:%M} ${font FiraCode Nerd Font Mono:size=72}┃
--┗━━━━━━━━━┛
--${voffset -400}${color 0033ff}${font FiraCode Nerd Font Mono:size=256}   
